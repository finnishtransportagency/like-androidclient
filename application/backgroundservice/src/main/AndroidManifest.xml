<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fi.livi.like.client.android.backgroundservice" >

    <!-- "main app manifest" has this, but lint requires it to be set also here on library -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application android:allowBackup="true"
        android:label="@string/app_name" >
        <service android:name="fi.livi.like.client.android.backgroundservice.service.BackgroundService"/>
    </application>
</manifest>
